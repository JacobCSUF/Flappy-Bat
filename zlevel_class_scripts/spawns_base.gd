class_name spawns_base
var spawn_location = 300

# Instantiate score point between walls
	#var score = score_scene.instantiate()
	#score.position = Vector2(spawn_location+23, offset)
	# add_child(score)
